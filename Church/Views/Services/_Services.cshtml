@model StaticPagedList<tblServices>

<div id="PagedServices">



    @foreach (tblServices item in Model)
    {
        <label> @item.Name</label>
        <label> @Html.ActionLink("Llevame a detalles ", "Details", new { id = item.ServiceID }, new { @class = "" })</label>
    }





</div>
